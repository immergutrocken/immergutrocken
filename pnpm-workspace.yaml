packages:
  - apps/*
  - packages/*
onlyBuiltDependencies:
  - '@vercel/speed-insights'
# Deliberately no minimumReleaseAge here — it makes Mend-hosted Renovate run out
# of memory. See the Package Manager section in CLAUDE.md before adding it back.
